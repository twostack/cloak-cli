.dart_tool/
build/
tool/scratch/
.claude/settings.local.json
# A machine-local symlink to ../tstokenlib/native, at the root only: a bare
# `native` also hid lib/src/native/ from git.
/native
# A person's own wallet, never in the repo.
*.enc
identity.seed
# Overrides to sibling checkouts, per machine.
pubspec_overrides.yaml
# Editor and Finder files, per machine.
.idea/
.DS_Store
